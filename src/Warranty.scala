object Warranty {

  //This object (read "class") simply takes a raw transaction sent in by a user
  //and signs it with the CoinJoin server's private key
  //This will provide the user of the server a "warranty" in case his/hers
  //Bitcoins get taken by the server
  
  //Take a hexadecimal raw transaction and sign it with the server's private key
  def signWarranty(transaction : String) = {
    
  }
}